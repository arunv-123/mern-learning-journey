let arr = [1,2,3,4,5,6,7,8,9]
res = arr.slice(2,5)//(strarting point, ending point)
console.log(res);

spli = arr.splice(3,2)//(starting point,count) splice will delte the cutted index from the whole array
console.log(spli);
